generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Message {
  id         String   @id @default(cuid())
  name       String
  text       String
  created_at DateTime @default(now()) @db.Timestamptz(6)

  @@map("messages")
}
