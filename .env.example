# PostgreSQL connection string for server-side persistence.
# Replace the placeholder values with your own database credentials.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zeroclaw_messages?sslmode=disable
